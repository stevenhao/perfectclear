{
  "name": "tetris2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build:wasm": "make -f wasm.mk",
    "build:coffee": "coffee -c public/*.coffee",
    "build": "npm run build:wasm && npm run build:coffee"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4"
  }
}
